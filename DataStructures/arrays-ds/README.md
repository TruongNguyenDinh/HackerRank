# Topic [Hacker Rank](https://www.hackerrank.com/challenges/arrays-ds/problem?isFullScreen=true) <- Click to here

```cpp
vector<int> reverseArray(vector<int> a) {
    reverse(a.begin(),a.end());
    return a;
}
```


